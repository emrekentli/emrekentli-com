import { ThemeToggle } from "@/components/theme-toggle";
const links = [["Hakkımda", "#about"], ["Projeler", "#projects"], ["Deneyim", "#experience"]] as const;
export function Navigation() { return <header className="site-header"><nav className="nav-shell" aria-label="Ana menü"><a className="brand" href="#content" aria-label="Emre Kentli ana sayfa">EK<span>.</span></a><div className="nav-links">{links.map(([label, href]) => <a href={href} key={href}>{label}</a>)}</div><div className="nav-actions"><ThemeToggle /><a className="nav-contact" href="#contact">İletişim <span aria-hidden="true">↗</span></a></div></nav></header>; }
